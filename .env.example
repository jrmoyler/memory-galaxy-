# Memory Galaxy — environment template
# V1 runs entirely on local mock data; these are placeholders for the
# Supabase-ready V2 data layer. Copy to `.env.local` and fill in when wiring
# a backend. Never commit real keys.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
